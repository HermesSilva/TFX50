//<auto-generated/>
using Microsoft.AspNetCore.Mvc;
using Microsoft.Extensions.Logging;
using TFX.Core.Controllers;
using TFX.Core.Services;
using System;
using TFX.Core.Model;
using Microsoft.AspNetCore.Http;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using TFX.Core.Data;
using Tootega.Core.ERP.PessoaFisica;
using Tootega.Core.ERP.PessoaFisica.Rules;
using TFX.Core.Data.DB;
using Tootega.Core.ERP.DB;

namespace Tootega.Core.ERP.PessoaFisica
{
    [Route("PessoaFisica")]
    [ApiController]
    public class PessoaFisicaController : XController
    {
        public abstract class BaseINFPessoaFisicaControllerRule : XControllerINFRule<PessoaFisicaController>
        {
            public BaseINFPessoaFisicaControllerRule(PessoaFisicaController pController)
                :base(pController)
            {
            }
        }

        public PessoaFisicaController(IPessoaFisicaService pService)
        {
            Service = pService;
            _Rule = new INFPessoaFisicaControllerRule(this);
        }

        internal readonly IPessoaFisicaService Service;
        private readonly INFPessoaFisicaControllerRule _Rule;

        [HttpPost("Search")]
        [ProducesResponseType(typeof(PessoaFisicaDataSet), StatusCodes.Status200OK)]
        [ProducesResponseType(typeof(XEndPointMessage), StatusCodes.Status404NotFound)]
        public IActionResult Execute([FromBody] PessoaFisicaFilter pFilter)
        {
            try
            {
                var dst = Service.Execute(pFilter, true);
                return Ok(dst);
            }
            catch (Exception pEx)
            {
                return StatusCode(404, XEndPointMessage.Erro(pEx));
            }  
        }

        [HttpPost("Flush")]
        [ProducesResponseType(typeof(PessoaFisicaDataSet), StatusCodes.Status200OK)]
        [ProducesResponseType(typeof(XEndPointMessage), StatusCodes.Status404NotFound)]
        [XEndpointDescription(typeof(PessoaFisicaService.CORxPessoa), typeof(PessoaFisicaService.ERPxPessoaFisica))]
        public IActionResult Flush([FromBody] PessoaFisicaDataSet pDataSet)
        {
            try
            {
                var result = Service.Flush(pDataSet);
                Service.GracefullyClose();
                return Ok(result);
            }
            catch (Exception pEx)
            {
                return StatusCode(404, XEndPointMessage.Erro(pEx));
            }  
        }
    }
}